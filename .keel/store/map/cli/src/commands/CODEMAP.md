---
id: MAP-cli-src-commands
scope: dir:cli/src/commands
owner: agent
verified_at: 2026-09-17
generated: true
---

# CODEMAP — `cli/src/commands`

<!-- generated by `keel map`. Do not edit: your changes will be overwritten. -->

16 files · 2161 lines · 136 symbols

**`cli/src/commands/mod.rs`** · 279 lines · imported by 1
- `pub mod audit_cmd;`  <sub>L1</sub>
- `pub mod down;`  <sub>L2</sub>
- `pub mod import_cmd;`  <sub>L3</sub>
- `pub mod keel_cmd;`  <sub>L4</sub>
- `pub mod logs;`  <sub>L5</sub>
- `fn running_projects(names: &[String]) -> Result<Vec<String>>` — running (`moor up`'d). A single `docker ps` covers every project at  <sub>L24</sub>
- `pub enum ProjectSource`  <sub>L48</sub>
- `fn pick_running_or_sole_project(` — once an explicit `--project` and a `moor use` default have both come  <sub>L70</sub>
- `pub fn resolve_project(explicit: Option<String>) -> Result<(String, Option<ProjectSource>)>` — one project at all, that one. Short-circuits before touching disk or  <sub>L100</sub>
- `pub fn resolve_project_announced(explicit: Option<String>) -> Result<String>` — appearing in the command line, so it's always visible which container  <sub>L117</sub>
- `pub fn create_github_repo_interactive(name: &str) -> Result<Option<String>>` — `import` so the confirm-then-create-then-report flow can't drift  <sub>L132</sub>
- `pub fn compose_up(name: &str) -> Result<()>` — Called by both `new` and `up` so a hand-edited moor.yaml always  <sub>L156</sub>
- `pub fn sync_git_identity(m: &Manifest)` — now exists call this again afterward) or if the host itself has no  <sub>L190</sub>
- `pub fn compose_down(name: &str) -> Result<()>`  <sub>L205</sub>

**`cli/src/commands/run_cmd.rs`** · 81 lines · imported by 2
- `fn looks_like_git_push(cmd: &[String]) -> bool` — stronger alternative (it runs inside the untrusted sandbox and the  <sub>L13</sub>
- `pub fn run(name: &str, cmd: &[String]) -> Result<()>`  <sub>L17</sub>
- `mod tests`  <sub>L42</sub>
- `fn detects_plain_git_push()`  <sub>L46</sub>
- `fn does_not_flag_other_git_commands()`  <sub>L57</sub>
- `fn does_not_flag_non_git_commands()`  <sub>L68</sub>
- `fn empty_command_is_not_a_push()`  <sub>L78</sub>

**`cli/src/commands/recipe.rs`** · 423 lines · imported by 1
- `struct FrontMatter`  <sub>L16</sub>
- `fn default_max_gate_retries() -> u32`  <sub>L28</sub>
- `fn default_max_run_attempts() -> u32`  <sub>L32</sub>
- `struct Recipe`  <sub>L37</sub>
- `fn parse_recipe(path: &Path) -> Result<Recipe>`  <sub>L42</sub>
- `struct NextSpec`  <sub>L68</sub>
- `struct NextReport`  <sub>L76</sub>
- `fn log_event(name: &str, event: &str, mut data: serde_json::Value) -> Result<()>` — command output, which can be large and is already in the "recipe"  <sub>L89</sub>
- `fn exec_capture(name: &str, m: &Manifest, argv: &[String]) -> Result<(bool, String)>`  <sub>L98</sub>
- `fn next_report(name: &str, m: &Manifest, slug: &str) -> Result<NextReport>`  <sub>L107</sub>
- `fn author_spec(name: &str, m: &Manifest, slug: &str, description: &str) -> Result<()>` — (verified directly: it built the feature and left the spec untouched  <sub>L124</sub>
- `fn drive_gate(` — used for the build step itself (`keel run`) — only for fixing the  <sub>L158</sub>
- `pub fn run(name: &str, recipe_path: &Path) -> Result<()>`  <sub>L203</sub>
- `mod tests`  <sub>L351</sub>

**`cli/src/commands/selftest.rs`** · 386 lines · imported by 1
- `struct Check`  <sub>L5</sub>
- `fn probe(` — — always evaluated as `false` if the probe itself couldn't be run at  <sub>L19</sub>
- `fn run_breakout_battery(container: &str) -> Vec<Check>`  <sub>L43</sub>
- `fn evaluate(info: &Value) -> Vec<Check>` — table in policies/README.md. Kept separate from `run()` so it can be  <sub>L89</sub>
- `pub fn run(name: &str) -> Result<()>` — isolation — see Phase 7 in the moor plan for the full breakout  <sub>L188</sub>
- `mod tests`  <sub>L247</sub>
- `fn hardened_container() -> Value` — A `docker inspect` result matching everything the compose template  <sub>L253</sub>
- `fn hardened_container_passes_every_check()`  <sub>L274</sub>
- `fn writable_rootfs_fails()`  <sub>L287</sub>
- `fn missing_cap_drop_all_fails()`  <sub>L299</sub>
- `fn missing_no_new_privileges_fails()`  <sub>L311</sub>
- `fn bind_mount_fails()`  <sub>L332</sub>
- `fn root_user_fails()`  <sub>L345</sub>
- `fn default_bridge_network_fails()`  <sub>L356</sub>

**`cli/src/commands/import_cmd.rs`** · 370 lines · imported by 1
- `fn detect_image(from: &Path) -> &'static str` — read-only existence checks — so it's unit-testable against a fixture  <sub>L9</sub>
- `fn parse_github_repo_slug(url: &str) -> Option<String>` — specifically, so a non-GitHub remote is left for the operator to note  <sub>L28</sub>
- `pub fn run(name: &str, from: &Path, image: Option<String>, github: bool) -> Result<()>`  <sub>L42</sub>
- `fn import_history(name: &str, m: &Manifest, from: &Path, real_remote: Option<&str>) -> Result<()>` — `docker cp`, cloned from inside the container, then deleted on both  <sub>L163</sub>
- `mod tests`  <sub>L268</sub>
- `fn temp_dir(label: &str) -> PathBuf`  <sub>L276</sub>
- `fn detects_rust_project()`  <sub>L287</sub>
- `fn detects_node_project()`  <sub>L295</sub>
- `fn detects_python_project_via_pyproject()`  <sub>L303</sub>
- `fn detects_python_project_via_requirements_txt()`  <sub>L311</sub>
- `fn falls_back_to_base_when_nothing_recognized()`  <sub>L319</sub>
- `fn rust_takes_priority_when_multiple_markers_present()`  <sub>L326</sub>
- `fn parses_https_github_url()`  <sub>L338</sub>
- `fn parses_ssh_github_url_forms()`  <sub>L350</sub>

**`cli/src/commands/view.rs`** · 127 lines · imported by 1
- `fn artifact_filename(artifact: &str) -> Result<&'static str>` — The three keel-produced artifacts `moor view` knows how to find,  <sub>L6</sub>
- `const BOLD: &str = "\x1b[1m";`  <sub>L15</sub>
- `const DIM: &str = "\x1b[2m";`  <sub>L16</sub>
- `const CYAN: &str = "\x1b[36m";`  <sub>L17</sub>
- `fn highlight(markdown: &str) -> String` — reads better in a terminal without pulling in a markdown-rendering  <sub>L25</sub>
- `mod tests`  <sub>L57</sub>
- `fn maps_known_artifacts_to_their_filename()`  <sub>L61</sub>
- `fn rejects_an_unknown_artifact()`  <sub>L68</sub>
- `fn highlights_headings_bold_cyan()`  <sub>L74</sub>
- `fn highlights_checkbox_lines_yellow()`  <sub>L80</sub>
- `fn dims_code_fence_contents_including_the_fences()`  <sub>L89</sub>
- `fn leaves_ordinary_lines_untouched()`  <sub>L98</sub>
- `fn a_heading_marker_inside_a_code_fence_is_dimmed_not_bolded()`  <sub>L104</sub>
- `pub fn run(project: &str, slug: &str, artifact: &str) -> Result<()>`  <sub>L113</sub>

**`cli/src/commands/use_cmd.rs`** · 57 lines · imported by 1
- `fn check_known(name: &str, names: &[String]) -> Result<()>` — known so the operator can correct a typo immediately. Split out from  <sub>L7</sub>
- `pub fn run(name: &str) -> Result<()>` — omitted, so switching your working project doesn't mean typing its  <sub>L25</sub>
- `mod tests`  <sub>L33</sub>
- `fn accepts_a_known_project()`  <sub>L37</sub>
- `fn rejects_an_unknown_project_and_lists_the_known_ones()`  <sub>L43</sub>
- `fn rejects_with_a_clear_message_when_there_are_no_projects_yet()`  <sub>L53</sub>

**`cli/src/commands/keel_cmd.rs`** · 56 lines · imported by 1
- `fn build_argv(args: &[String]) -> Result<Vec<String>>` — whatever the operator typed. Pulled out from `run` so the "keel goes  <sub>L7</sub>
- `pub fn run(project: &str, args: &[String]) -> Result<()>` — disambiguate when more than one project exists) drops both the `--`  <sub>L21</sub>
- `mod tests`  <sub>L27</sub>
- `fn prepends_keel_to_the_given_args()`  <sub>L31</sub>
- `fn empty_args_is_an_error()`  <sub>L37</sub>
- `fn preserves_hyphenated_flags_in_order()`  <sub>L43</sub>

**`cli/src/commands/logs.rs`** · 95 lines · imported by 1
- `fn format_entry(line: &str) -> Option<String>` — stray malformed line can't crash what's meant to be a lightweight  <sub>L13</sub>
- `fn describe(data: &Value) -> String`  <sub>L28</sub>
- `pub fn run(name: &str, follow: bool, lines: usize) -> Result<()>`  <sub>L58</sub>

**`cli/src/commands/audit_cmd.rs`** · 105 lines · imported by 1
- `pub fn run(name: &str, verify: bool, export: Option<PathBuf>) -> Result<()>` — printed reflects what's actually happened, not just what `moor  <sub>L9</sub>
- `fn export_bundle(name: &str, m: &Manifest, out_dir: PathBuf) -> Result<PathBuf>`  <sub>L68</sub>

**`cli/src/commands/new_cmd.rs`** · 93 lines · imported by 1
- `pub fn run(name: &str, image: &str, github: bool) -> Result<()>`  <sub>L4</sub>

**`cli/src/commands/status.rs`** · 38 lines · imported by 1
- `pub fn run() -> Result<()>`  <sub>L4</sub>

**`cli/src/commands/secrets_status.rs`** · 21 lines · imported by 1
- `pub fn run(project: &str) -> Result<()>`  <sub>L4</sub>

**`cli/src/commands/shell.rs`** · 12 lines · imported by 1
- `pub fn run(name: &str) -> Result<()>`  <sub>L4</sub>

**`cli/src/commands/up.rs`** · 11 lines · imported by 1
- `pub fn run(name: &str) -> Result<()>`  <sub>L4</sub>

**`cli/src/commands/down.rs`** · 7 lines · imported by 1
- `pub fn run(name: &str) -> Result<()>`  <sub>L3</sub>

